@model WebApp.ViewModels.SignInViewModel

@{
    ViewData["Title"] = "Logga in";
    Layout = "_CleanLayout";
}

<div class="relative flex min-h-dvh items-center justify-center px-4 md:px-0">
    
    <div class="w-full max-w-md">
        <a asp-controller="Home"
           asp-action="Index"
           class="back-btn">
            ← Till startsidan
        </a>

        @await Html.PartialAsync("Partials/Forms/_SignInForm")
    </div>
</div>